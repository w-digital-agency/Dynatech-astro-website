---
/**
 * Hero Component for Dynatech
 *
 * @description Hero section showcasing Dynatech's AI coaching and consulting services
 */
---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <h1 class="hero-title">
        Unlock AI Potential for Your <span class="text-gradient">Business</span>
      </h1>
      <p class="hero-description">
        Leading AI coaching and consulting in Hong Kong. Transform your business with cutting-edge 
        artificial intelligence solutions and expert guidance from our team of AI specialists.
      </p>
      <div class="hero-actions">
        <a href="/contact" class="button primary">
          Start Your AI Journey
        </a>
        <a href="/about" class="button secondary">
          Learn About Us
        </a>
      </div>
      <div class="hero-stats">
        <div class="stat">
          <span class="stat-number">100+</span>
          <span class="stat-label">Projects Completed</span>
        </div>
        <div class="stat">
          <span class="stat-number">50+</span>
          <span class="stat-label">Happy Clients</span>
        </div>
        <div class="stat">
          <span class="stat-number">5+</span>
          <span class="stat-label">Years Experience</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style lang="scss">
  .hero {
    padding: 6rem 0;
    background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
    border-bottom: 1px solid #e2e8f0;

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1rem;
    }

    .hero-content {
      text-align: center;
      max-width: 800px;
      margin: 0 auto;
    }

    .hero-title {
      font-size: 3.5rem;
      font-weight: 800;
      line-height: 1.1;
      margin-bottom: 1.5rem;
      color: #1f2937;

      .text-gradient {
        background: linear-gradient(135deg, #2563eb, #7c3aed);
        background-clip: text;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
      }

      @media (max-width: 768px) {
        font-size: 2.5rem;
      }
    }

    .hero-description {
      font-size: 1.25rem;
      line-height: 1.6;
      color: #6b7280;
      margin-bottom: 2rem;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }

    .hero-actions {
      display: flex;
      gap: 1rem;
      justify-content: center;
      margin-bottom: 3rem;

      @media (max-width: 480px) {
        flex-direction: column;
        align-items: center;
      }

      .button {
        display: inline-block;
        padding: 1rem 2rem;
        border-radius: 0.5rem;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
        font-size: 1.1rem;

        &.primary {
          background: #2563eb;
          color: white;
          border: 2px solid #2563eb;
        }

        &.primary:hover {
          background: #1d4ed8;
          border-color: #1d4ed8;
          transform: translateY(-2px);
        }

        &.secondary {
          background: transparent;
          color: #2563eb;
          border: 2px solid #2563eb;
        }

        &.secondary:hover {
          background: #2563eb;
          color: white;
        }
      }
    }

    .hero-stats {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
      max-width: 500px;
      margin: 0 auto;

      @media (max-width: 600px) {
        grid-template-columns: 1fr;
        gap: 1rem;
      }

      .stat {
        text-align: center;

        .stat-number {
          display: block;
          font-size: 2rem;
          font-weight: 700;
          color: #1f2937;
          margin-bottom: 0.25rem;
        }

        .stat-label {
          display: block;
          font-size: 0.875rem;
          color: #6b7280;
          text-transform: uppercase;
          letter-spacing: 0.05em;
        }
      }
    }
  }

  @media (prefers-color-scheme: dark) {
    .hero {
      background: linear-gradient(135deg, #1e293b 0%, #0f172a 100%);

      .hero-title {
        color: #f9fafb;
      }

      .hero-description {
        color: #d1d5db;
      }

      .stat-number {
        color: #f9fafb !important;
      }

      .stat-label {
        color: #d1d5db !important;
      }
    }
  }
</style>
